# Usado APENAS pelo Prisma para escrever migrações (`npm run db:migrar`), contra
# um SQLite local descartável. A aplicação não lê esta variável: em execução ela
# fala com o D1 pelo binding do Worker.
DATABASE_URL="file:./dev.db"
